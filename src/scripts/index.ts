window.addEventListener('DOMContentLoaded', () => handleDOMContentLoaded());

const handleDOMContentLoaded = async () => {
  // customElements.define('market-status-desktop', MarketStatusDesktop);

  // add Lit component
  // document
  //   .querySelector('main')
  //   .prepend(document.createElement('simple-greeting'));
};

// const getElementByTagName = (tag) => document.getElementsByTagName(tag)[0];

// const handleResize = () => {
//   const isMobile = matchMedia('(max-width: 768px)').matches;
//   // const marketStatusDesktop = getElementByTagName('market-status-desktop');

//   // marketStatusDesktop.setAttribute('is-mobile', isMobile);
// };

// window.addEventListener('resize', handleResize);
